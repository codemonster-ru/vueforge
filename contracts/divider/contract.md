# Divider contract

Status: Active

Divider renders one native `hr.cm-divider` with `cm-divider--{orientation}`, `role=separator`, and
an explicit matching `aria-orientation`. It has no content, interaction, or managed focus. Consumer
root attributes and classes are preserved, while contract attributes cannot be overridden.
