{
  "schemaVersion": 1,
  "name": "Tabs",
  "slug": "tabs",
  "razorTag": "cm-tabs",
  "description": "Automatically activated horizontal tabs with related panels.",
  "props": {
    "id": { "type": "string", "required": true },
    "items": { "type": "collection", "required": true, "valueContract": "tab-item" },
    "value": {
      "type": "nullable",
      "required": false,
      "default": null,
      "valueContract": "tab-value"
    },
    "defaultValue": {
      "type": "nullable",
      "required": false,
      "default": null,
      "valueContract": "tab-value"
    }
  },
  "slots": {
    "tab{UpperCamelValue}": {
      "required": false,
      "description": "Trusted content inside an item tab.",
      "scopeContract": "tab-item-state"
    },
    "panel{UpperCamelValue}": {
      "required": false,
      "description": "Trusted content inside an item panel.",
      "scopeContract": "tab-item-state"
    }
  },
  "events": {
    "valueChange": { "description": "Reports the selected enabled tab value.", "payloadContract": "tab-value" }
  },
  "states": ["default", "selected", "disabled"]
}
