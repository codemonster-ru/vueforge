{
  "schemaVersion": 1,
  "name": "Container",
  "slug": "container",
  "razorTag": "cm-container",
  "description": "A centered responsive inline-size constraint with stable gutters.",
  "props": {
    "element": { "type": "enum", "required": false, "default": "div", "values": ["div", "main", "section"] },
    "size": { "type": "nullable", "required": false, "default": null, "valueContract": "container-size" },
    "fluid": { "type": "boolean", "required": false, "default": false }
  },
  "slots": { "default": { "required": true, "description": "Trusted container content." } },
  "events": {},
  "states": ["default", "md", "lg", "xl", "2xl", "fluid"]
}
