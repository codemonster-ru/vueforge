{
  "schemaVersion": 1,
  "name": "DataTable",
  "slug": "data-table",
  "razorTag": "cm-data-table",
  "description": "A server-friendly tabular data view with sorting, row selection, and pagination requests.",
  "props": {
    "id": { "type": "string", "required": true },
    "columns": { "type": "collection", "required": true, "valueContract": "data-table-column" },
    "rows": { "type": "collection", "required": false, "default": [], "valueContract": "data-table-row" },
    "caption": { "type": "string", "required": false, "default": "" },
    "density": { "type": "enum", "required": false, "default": "default", "values": ["default", "compact"] },
    "striped": { "type": "boolean", "required": false, "default": false },
    "columnDividers": { "type": "boolean", "required": false, "default": false },
    "stickyHeader": { "type": "boolean", "required": false, "default": false },
    "visibleColumnKeys": {
      "type": "nullable",
      "required": false,
      "default": null,
      "valueContract": "ordered-data-table-column-keys"
    },
    "selectable": { "type": "boolean", "required": false, "default": false },
    "selectedRowIds": { "type": "collection", "required": false, "default": [], "valueContract": "data-table-row-id" },
    "sort": { "type": "nullable", "required": false, "default": null, "valueContract": "data-table-sort" },
    "page": { "type": "number", "required": false, "default": 1 },
    "pageCount": { "type": "number", "required": false, "default": 1 },
    "pageSize": { "type": "number", "required": false, "default": 10 },
    "pageSizeOptions": { "type": "collection", "required": false, "default": [], "valueContract": "positive-integers" },
    "totalRows": { "type": "nullable-number", "required": false, "default": null },
    "loading": { "type": "boolean", "required": false, "default": false },
    "error": { "type": "boolean", "required": false, "default": false },
    "emptyText": { "type": "string", "required": false, "default": "No data" },
    "loadingText": { "type": "string", "required": false, "default": "Loading..." },
    "errorText": { "type": "string", "required": false, "default": "Failed to load data" },
    "paginationLabel": { "type": "string", "required": false, "default": "Table pagination" },
    "rowsPerPageLabel": { "type": "string", "required": false, "default": "Rows per page" },
    "pageSummaryTemplate": { "type": "string", "required": false, "default": "Page {page} of {pageCount}" },
    "paginationSummaryTemplate": {
      "type": "string",
      "required": false,
      "default": "{firstRow}-{lastRow} of {totalRows}"
    },
    "emptyPaginationSummaryText": { "type": "string", "required": false, "default": "0 rows" },
    "previousPageText": { "type": "string", "required": false, "default": "Previous" },
    "nextPageText": { "type": "string", "required": false, "default": "Next" },
    "previousPageLabel": { "type": "string", "required": false, "default": "Previous page" },
    "nextPageLabel": { "type": "string", "required": false, "default": "Next page" },
    "selectAllLabel": { "type": "string", "required": false, "default": "Select all rows" },
    "selectRowLabelTemplate": { "type": "string", "required": false, "default": "Select {row}" },
    "sortAscendingLabelTemplate": { "type": "string", "required": false, "default": "Sort {column} ascending" },
    "sortDescendingLabelTemplate": {
      "type": "string",
      "required": false,
      "default": "Sort {column} descending"
    },
    "clearSortLabelTemplate": { "type": "string", "required": false, "default": "Clear sorting for {column}" }
  },
  "slots": {},
  "events": {
    "sortChange": {
      "description": "Reports the next single-column sort state or null.",
      "payloadContract": "nullable-data-table-sort"
    },
    "selectionChange": {
      "description": "Reports selected row ids in rendered row order.",
      "payloadContract": "data-table-row-ids"
    },
    "pageChange": { "description": "Reports the requested one-based page.", "payloadContract": "page-number" },
    "pageSizeChange": { "description": "Reports the requested positive page size.", "payloadContract": "page-size" }
  },
  "states": ["default", "sorted", "selected", "paginated", "empty", "loading", "error"]
}
