<!-- prettier-ignore -->
<dialog class="{{ $classes }}" id="{{ $id }}-drawer" aria-labelledby="{{ $id }}-title"@if ($description !== null && $description !== '') aria-describedby="{{ $id }}-description"@endif data-cm-controller="drawer" data-cm-drawer-state="{{ $open ? 'open' : 'closed' }}"@if ($open) open@endif{!! $attributes !!}><div class="cm-drawer__surface"><header class="cm-drawer__header"><h2 class="cm-drawer__title" id="{{ $id }}-title">{{ $title }}</h2><button class="cm-drawer__close" type="button" aria-label="{{ $closeLabel }}" data-cm-drawer-close>×</button></header>@if ($description !== null && $description !== '')<p class="cm-drawer__description" id="{{ $id }}-description">{{ $description }}</p>@endif<div class="cm-drawer__body">{{ $body }}</div>@if ($footer !== null)<footer class="cm-drawer__footer">{{ $footer }}</footer>@endif</div></dialog>
