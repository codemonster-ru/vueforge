<div class="{{ $classes }}" role="progressbar" aria-label="{{ $label }}"@if (!$indeterminate) aria-valuemin="0" aria-valuemax="{{ $max }}" aria-valuenow="{{ $value }}"@endif{!! $attributes !!}><span class="cm-progress-bar__value"@if (!$indeterminate) style="inline-size: {{ $percentage }}%"@endif>@if ($showValue && !$indeterminate)<span class="cm-progress-bar__label">{{ $valueLabel }}</span>@endif</span></div>
