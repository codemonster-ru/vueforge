<!-- prettier-ignore -->
<div class="{{ $classes }}" data-cm-controller="popover"{!! $attributes !!}><button class="cm-popover__trigger" id="{{ $id }}-trigger" type="button" aria-expanded="{{ $open ? 'true' : 'false' }}" aria-controls="{{ $id }}-panel"@if ($disabled) disabled@endif>{{ $label }}</button><div class="cm-popover__panel" id="{{ $id }}-panel" role="dialog" aria-labelledby="{{ $id }}-trigger"@if (!$open) hidden@endif>{{ $content }}</div></div>
