<div class="{{ $classes }}" data-cm-controller="tabs" data-cm-tabs-value="{{ $activeValue }}"{!! $attributes !!}><div class="cm-tabs__list" role="tablist" aria-orientation="horizontal">@foreach ($items as $item)<button class="cm-tabs__tab" id="{{ $id }}-tab-{{ $item['value'] }}" type="button" role="tab" aria-controls="{{ $id }}-panel-{{ $item['value'] }}" aria-selected="{{ $item['active'] ? 'true' : 'false' }}" tabindex="{{ $item['active'] ? '0' : '-1' }}"@if ($item['disabled']) disabled@endif>{{ $item['label'] }}</button>@endforeach</div>@foreach ($items as $item)<div class="cm-tabs__panel" id="{{ $id }}-panel-{{ $item['value'] }}" role="tabpanel" aria-labelledby="{{ $id }}-tab-{{ $item['value'] }}" tabindex="0"@if (!$item['active']) hidden@endif>{{ $item['content'] }}</div>@endforeach</div>
