<!-- prettier-ignore -->
<dialog class="{{ $classes }}" id="{{ $id }}-dialog" aria-labelledby="{{ $id }}-title"@if ($description !== null && $description !== '') aria-describedby="{{ $id }}-description"@endif data-cm-controller="dialog" data-cm-dialog-state="{{ $open ? 'open' : 'closed' }}"@if ($open) open@endif{!! $attributes !!}><div class="cm-dialog__surface"><header class="cm-dialog__header"><h2 class="cm-dialog__title" id="{{ $id }}-title">{{ $title }}</h2><button class="cm-dialog__close" type="button" aria-label="{{ $closeLabel }}" data-cm-dialog-close>×</button></header>@if ($description !== null && $description !== '')<p class="cm-dialog__description" id="{{ $id }}-description">{{ $description }}</p>@endif<div class="cm-dialog__body">{{ $body }}</div>@if ($footer !== null)<footer class="cm-dialog__footer">{{ $footer }}</footer>@endif</div></dialog>
