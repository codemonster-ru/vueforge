@if ($isLink)<a class="{{ $classes }}"@if (!$isDisabled) href="{{ $href }}"@else role="link" aria-disabled="true"@endif@if ($loading) aria-busy="true"@endif{!! $attributes !!}>@else<button class="{{ $classes }}" type="{{ $type }}"@if ($isDisabled) disabled@endif@if ($loading) aria-busy="true"@endif{!! $attributes !!}>@endif@if ($loading)<span class="cm-button__spinner" aria-hidden="true"><svg class="cm-button__spinner-svg" viewBox="0 0 50 50" focusable="false"><circle class="cm-button__spinner-track" cx="25" cy="25" r="20" fill="none" stroke-width="5"></circle><circle class="cm-button__spinner-value" cx="25" cy="25" r="20" fill="none" stroke-width="5"></circle></svg></span>@elseif ($leading !== null)<span class="cm-button__leading">{{ $leading }}</span>@endif<span class="cm-button__label">{{ $label }}</span>@if ($trailing !== null)<span class="cm-button__trailing">{{ $trailing }}</span>@endif@if ($isLink)</a>@else</button>@endif
