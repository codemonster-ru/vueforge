@if ($isLink)<a class="{{ $classes }}"@if (!$isDisabled) href="{{ $href }}"@else role="link" aria-disabled="true"@endif@if ($loading) aria-busy="true"@endif{!! $attributes !!}>@else<button class="{{ $classes }}" type="{{ $type }}"@if ($isDisabled) disabled@endif@if ($loading) aria-busy="true"@endif{!! $attributes !!}>@endif@if ($loading)<span class="cm-button__spinner" aria-hidden="true"></span>@elseif ($leading !== null)<span class="cm-button__leading">{{ $leading }}</span>@endif<span class="cm-button__label">{{ $label }}</span>@if ($trailing !== null)<span class="cm-button__trailing">{{ $trailing }}</span>@endif@if ($isLink)</a>@else</button>@endif
