# Planned Component Compliance Matrix

The planned matrix was reset after the package surface was narrowed to `core + layouts`.

## Current policy

- Add rows only for components that are intended to ship from `@codemonster-ru/vueforge` or `@codemonster-ru/vueforge-layouts`.
- Do not keep rows for removed chart/block families as historical backlog.
