# Theming

`VfConfirmDialog` composes `VfDialog` and `VfButton`; their theme tokens remain the styling
contract. `.vf-confirm-dialog__description` styles body copy, while
`.vf-confirm-dialog__actions` controls wrapping and alignment of footer actions.
